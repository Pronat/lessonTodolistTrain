export const todolistsReducer = (state: any, action: any) => {
    switch (action.type) {
        case 'XXX':
            return state
        default:
            throw new Error(`Invalid type`)
    }
}